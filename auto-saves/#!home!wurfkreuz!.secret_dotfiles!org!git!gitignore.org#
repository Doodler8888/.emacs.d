* Examples

<s
* Gotchas
** Retroactivity of gitignore and commiting mechanism
 .gitignore isn't retroactive _(?)_. That means, if i already track something,
 then in order to activate gitingonre instruction on that ingnored file i need
 to delete it from the local repo _(?)_.

* Selective ignoring within a non-root directory

Let's consider a situation where you need to ignore certain files in a
directory called 'eshell' which is placed in the root.

* Good practices
** Consistent negation for recursion
 Follow  consistent negation rules by adding '\*' or '\**' if you need to target
 something specific inside a directory.
